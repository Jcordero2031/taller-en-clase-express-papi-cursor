{
  "name": "taller-xpress",
  "version": "1.0.0",
  "private": true,
  "description": "Taller Express: /login (JWT) y /request por roles",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "vercel:dev": "vercel dev",
    "vercel:deploy": "vercel deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "vercel": "^47.0.5"
  }
}
